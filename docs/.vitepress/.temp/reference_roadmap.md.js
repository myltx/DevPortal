import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"DevPortal 后续优化清单（计划）","description":"","frontmatter":{},"headers":[],"relativePath":"reference/roadmap.md","filePath":"reference/roadmap.md","lastUpdated":1777286206000}');
const _sfc_main = { name: "reference/roadmap.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="devportal-后续优化清单-计划" tabindex="-1">DevPortal 后续优化清单（计划） <a class="header-anchor" href="#devportal-后续优化清单-计划" aria-label="Permalink to &quot;DevPortal 后续优化清单（计划）&quot;">​</a></h1><p>说明：此文件仅用于记录后续要做的任务与验收标准；<strong>在你明确说“开始做第 X 项”之前，不进入代码开发</strong>。</p><h2 id="优先级约定" tabindex="-1">优先级约定 <a class="header-anchor" href="#优先级约定" aria-label="Permalink to &quot;优先级约定&quot;">​</a></h2><ul><li><strong>P0</strong>：必须做 / 高收益 / 风险控制</li><li><strong>P1</strong>：体验增强（效率与可用性）</li><li><strong>P2</strong>：质量与长期维护（可持续迭代）</li><li><strong>P3</strong>：流程与运维（减少踩坑）</li></ul><h2 id="待办任务-17-项" tabindex="-1">待办任务（17 项） <a class="header-anchor" href="#待办任务-17-项" aria-label="Permalink to &quot;待办任务（17 项）&quot;">​</a></h2><h3 id="p0-必须做-高收益" tabindex="-1">P0（必须做/高收益） <a class="header-anchor" href="#p0-必须做-高收益" aria-label="Permalink to &quot;P0（必须做/高收益）&quot;">​</a></h3><ul><li><p>[x] <strong>P0-1 文本 Tab 不默认展示解析预览表</strong></p><ul><li>目标：文本 Tab 以“原文查看”为主，解析预览统一放到“批量导入弹窗”里。</li><li>验收： <ul><li>文本 Tab 顶部只保留“识别/已存在/可导入”汇总 + 导入按钮（从当前文本/粘贴覆盖）。</li><li>不在文本 Tab 展示解析预览表格（或默认折叠且不占空间）。</li><li>解析预览与编辑仍可在“批量导入弹窗”完成。</li></ul></li></ul></li><li><p>[x] <strong>P0-2 部署脚本强校验扩展 Key</strong></p><ul><li>目标：避免上线后插件报 <code>Server not configured</code>。</li><li>验收： <ul><li><code>server-deploy.sh</code> 在首次部署/更新前检查 <code>.env</code> 是否包含 <code>DEVPORTAL_EXTENSION_API_KEY</code>。</li><li>缺失时明确提示需要配置，并给出示例值/说明。</li></ul></li></ul></li><li><p>[ ] <strong>P0-3 账号唯一性约束（DB 层）【延期/可选】</strong></p><ul><li>说明：当前为内部系统，暂不强制 DB 唯一；先依赖应用层去重与“已存在/重复”提示，允许少量重复由用户手动清理。</li><li>目标（如后续需要再做）：彻底杜绝并发/多端导入导致重复账号。</li><li>验收（如后续需要再做）： <ul><li>数据库添加唯一索引：<code>account(module_id, account, password)</code>。</li><li>导入/新增若触发冲突，前端提示友好（不影响其它成功项）。</li></ul></li></ul></li></ul><h3 id="p1-体验增强" tabindex="-1">P1（体验增强） <a class="header-anchor" href="#p1-体验增强" aria-label="Permalink to &quot;P1（体验增强）&quot;">​</a></h3><ul><li><p>[x] <strong>P1-4 批量导入弹窗：状态筛选/仅看“将导入”</strong></p><ul><li>目标：数据多时更快定位需要导入的条目。</li><li>验收： <ul><li>支持筛选：将导入 / 已存在 / 重复 / 无效（至少“仅看将导入”）。</li></ul></li></ul></li><li><p>[x] <strong>P1-5 单条导入后行内即时状态更新</strong></p><ul><li>目标：用户导入一条后能立刻看到该条变更（无需心里记忆）。</li><li>验收： <ul><li>单条导入成功后，该行状态立即变为“已存在”，并禁用“导入”按钮（或自动移出“将导入”视图）。</li></ul></li></ul></li><li><p>[x] <strong>P1-6 批次备注策略更清晰（文案/开关）</strong></p><ul><li>目标：避免用户误解“会覆盖已有账号备注/描述”。</li><li>验收： <ul><li>在导入弹窗中明确说明：仅新增写入 <code>remark/accountInfo</code>，不会覆盖已存在记录（或提供可选策略）。</li></ul></li></ul></li><li><p>[x] <strong>P1-7 导入弹窗支持“拆分一条为多条”</strong></p><ul><li>目标：当解析把多条合成一条时，可在导入弹窗里手动拆分成多条不同账号/密码。</li><li>验收： <ul><li>每行提供“拆分”入口，支持粘贴多行内容并解析成多条。</li><li>支持格式：<code>账号 密码</code>（空格/Tab）、<code>账号/密码</code>、两行一组 <code>账号\\n密码</code>。</li><li>拆分后条目可编辑、可单条导入/整批导入，且仍按状态标记与筛选工作。</li></ul></li></ul></li><li><p>[ ] <strong>P1-8 深色模式体验：ProjectSite 环境色更柔和</strong></p><ul><li>背景：当前默认浅色为主，但深色模式仍应保持可读与一致；避免环境 Tag/跳转按钮在深色下过亮刺眼。</li><li>目标：在 <code>ProjectSite</code> 中让环境 Tag 与主操作色在深色模式下更协调，同时保留“快速识别”的语义色。</li><li>验收： <ul><li><code>app/projectSite/ProjectSiteContent.tsx</code> 的环境 Tag/跳转按钮在浅色/深色均清晰且不突兀（对比度足够）。</li><li>避免在 <code>ProjectSite</code> 里新增零散硬编码色；优先使用 Antd token（语义色）或统一的 CSS 变量。</li></ul></li></ul></li><li><p>[x] <strong>P1-9 Apifox 推送日志：按项目保留最近 10 条</strong></p><ul><li>背景：Apifox 日志用于快速查看“最近推送是否成功”，无审计要求；按天保留在高频推送场景下不稳定。</li><li>目标：控制日志表增长，并让首页/列表始终保留“每个项目最新的一小段历史”用于排查。</li><li>验收： <ul><li>对每个项目仅保留最近 10 条推送日志（超出部分按时间从旧到新清理）。</li><li>清理过程分批执行，避免长时间锁表/影响正常写入。</li><li>清理任务可配置开关（是否启用）与保留条数（默认 10）。</li></ul></li></ul></li><li><p>[x] <strong>P1-10 Apifox 日志页：手动清理入口（按项目保留 N 条）</strong></p><ul><li>背景：虽然已支持“写入即清理”，但运维/排障时仍需要一个可控的“手动清理”入口，尤其用于历史数据一次性瘦身。</li><li>目标：在 Apifox 日志页面提供安全的清理操作入口，并展示清理结果摘要。</li><li>验收： <ul><li>在 <code>/apifox-logs</code> 页面提供“清理日志”按钮（带二次确认）。</li><li>触发后执行一次全库清理：对每个项目保留最近 <code>DEVPORTAL_APIFOX_LOG_KEEP_PER_PROJECT</code> 条（默认 10）。</li><li>返回并展示清理摘要（清理了多少条、涉及多少项目、耗时/时间戳）。</li><li>提供最小权限/安全限制（例如仅管理员可见/需服务端校验开关），避免误操作或被滥用。</li></ul></li></ul></li></ul><h3 id="p2-质量与长期维护" tabindex="-1">P2（质量与长期维护） <a class="header-anchor" href="#p2-质量与长期维护" aria-label="Permalink to &quot;P2（质量与长期维护）&quot;">​</a></h3><ul><li><p>[x] <strong>P2-7 <code>accountInfo</code> 推荐填写规范</strong></p><ul><li>目标：让插件/列表能稳定区分多账号。</li><li>验收： <ul><li>给出推荐格式与示例（如：<code>系统-角色-地区</code>），并在 UI 提示/占位符中体现。</li></ul></li></ul></li><li><p>[ ] <strong>P2-8 解析规则迭代机制（样例库/黑白名单）</strong></p><ul><li>目标：让“文本解析”可持续提升，不靠拍脑袋改规则。</li><li>验收： <ul><li>形成样例收集方式（失败样例/误判样例）。</li><li>支持最小的规则迭代（黑名单/白名单或特殊格式优先级）。</li></ul></li></ul></li><li><p>[ ] <strong>P2-9 插件填充失败兜底（手动选择输入框）</strong></p><ul><li>目标：复杂登录页也能用（失败时可控）。</li><li>验收： <ul><li>自动填充失败时，提供“选择账号框/密码框”的交互兜底（只在失败时出现）。</li></ul></li></ul></li><li><p>[ ] <strong>P2-11 ESLint/类型治理（清理现存报错 + 规则合理化）</strong></p><ul><li>背景：当前项目存在较多 <code>any</code>、hooks 规则等 lint 报错；并且需要确保构建产物目录不会被扫描（如 <code>.next-prod</code>）。</li><li>目标：让 <code>pnpm lint</code> 可持续作为质量门禁，而不是“每次都大量报错”。</li><li>验收： <ul><li><code>.next-prod</code> 等构建产物已从 lint 扫描中排除（忽略规则到位）。</li><li>逐步减少 TS/Hook 相关报错数量（按模块分批清理），不阻塞主功能迭代。</li><li>明确哪些规则必须遵守、哪些允许临时放行（有清晰的 TODO/时间点）。</li></ul></li></ul></li><li><p>[ ] <strong>P2-12 模块地址可达性检测（延期/可选）</strong></p><ul><li>背景：模块 <code>moduleUrl</code> 同时存在内网/外网；若部署服务器无法访问内网地址，定时检测会产生大量“误报”并增加运维成本。</li><li>目标：在明确网络可达性前置条件与安全策略（避免 SSRF）后，再引入“地址可达性检测”能力。</li><li>验收（如后续决定开发）： <ul><li>定义“可达”口径：HTTP 可响应（2xx/3xx/401/403）视为可达；DNS/连接/超时/TLS 失败视为不可达（404 口径待定）。</li><li>明确网络前置：服务器对内网地址的连通性与域名白名单策略（仅 http/https）。</li><li>优先支持手动检测（按钮触发 + 结果展示），再考虑定时全量检测与并发控制。</li></ul></li></ul></li><li><p>[ ] <strong>P2-13 /middle 与后台视觉体系统一（延期/可选）</strong></p><ul><li>背景：当前 <code>/middle</code> 使用 Tailwind + 自定义 <code>surface-card</code>，而后台页基于 Ant Design Layout/Card；整体易出现“像两个产品”的割裂感。</li><li>目标：统一边距、背景、卡片与按钮的视觉语言（可选方案：让 <code>/middle</code> 也进入 <code>MainLayout</code>；或抽象统一的 token/CSS 变量并复用）。</li><li>验收： <ul><li><code>/middle</code> 与后台页在浅色/深色下的背景与卡片层级一致（不突兀）。</li><li>Header / Content 的留白规则统一（不出现一页紧一页松）。</li><li>交互反馈一致（hover/active/cursor 等）。</li></ul></li></ul></li></ul><h3 id="p3-流程与运维" tabindex="-1">P3（流程与运维） <a class="header-anchor" href="#p3-流程与运维" aria-label="Permalink to &quot;P3（流程与运维）&quot;">​</a></h3><ul><li><p>[ ] <strong>P3-10 发布前检查清单</strong></p><ul><li>目标：把“容易踩坑的点”沉淀成流程。</li><li>验收： <ul><li>一份可执行的检查项：Prisma generate、build、环境变量、插件 Key 一致性、容器重建方式等。</li></ul></li></ul></li><li><p>[ ] <strong>P3-11 Apifox 日志清理：cron 定时任务（可选）</strong></p><ul><li>背景：多数情况下“写入即清理”已足够；但在低频推送/历史积累场景，定时任务可作为兜底手段。</li><li>目标：提供一份可复制的 cron 示例，用于定时执行 <code>pnpm cleanup:apifox-logs</code>。</li><li>验收： <ul><li>文档提供 cron 示例（如每天凌晨 3 点执行），并说明日志输出/失败告警建议。</li><li>明确前置：服务器能访问数据库，且 <code>DEVPORTAL_APIFOX_LOG_CLEANUP_ENABLED=true</code>。</li></ul></li></ul></li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/roadmap.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const roadmap = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  roadmap as default
};
