import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"项目部署文档","description":"","frontmatter":{},"headers":[],"relativePath":"guide/deploy.md","filePath":"guide/deploy.md","lastUpdated":1777308504000}');
const _sfc_main = { name: "guide/deploy.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="项目部署文档" tabindex="-1">项目部署文档 <a class="header-anchor" href="#项目部署文档" aria-label="Permalink to &quot;项目部署文档&quot;">​</a></h1><div class="important custom-block github-alert"><p class="custom-block-title">IMPORTANT</p><p>由于服务器操作系统较旧 (Glibc 版本过低)，无法直接运行 Node.js 20。 <strong>我们已切换为 Docker 容器化部署方案</strong>。这可以完美避开系统环境不兼容的问题。</p></div><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p>当前 <code>Dockerfile</code> <strong>不会在容器里执行 <code>next build</code></strong>，而是直接复制本地生成的 <code>.next-prod/standalone</code> 运行时产物，并额外复制 <code>.next-prod/static</code>。 因此无论你选择“离线镜像包部署”还是“服务器端 docker build”，都必须先在本地完成 <code>pnpm build:prod</code>（或直接运行 <code>pnpm docker:pack</code>）。</p><p>同时，Docker 构建会在中间层单独生成 Linux 版本的 Prisma / sharp 原生运行时文件，再只把这些必要目录复制到最终镜像中，因此最终镜像会明显小于“容器内完整安装依赖”的旧方案。</p><p>使用 <code>docker compose</code> 的 <code>env_file</code> 读取 <code>.env</code> 时，请不要把值写成 <code>KEY=&quot;value&quot;</code> 这种带双引号的形式，尤其是 <code>DATABASE_URL</code>。 例如要写成 <code>DATABASE_URL=mysql://user:pass@host:3306/db</code>，否则某些运行时会把双引号也当成值的一部分，进而导致 Prisma 认为连接串不是以 <code>mysql://</code> 开头。</p></div><div class="tip custom-block github-alert"><p class="custom-block-title">TIP</p><p>本仓库当前默认采用的是 <strong>“Mac/本机构建 Next 产物 + Docker 内补齐 Linux 原生依赖”</strong> 策略。 这样做的主要原因是：在 Apple Silicon Mac 上直接使用 <code>docker buildx --platform linux/amd64</code> 执行 <code>next build</code>，通常会落到 QEMU 跨架构模拟，构建速度慢、稳定性也较差。</p><p>如果你是开源使用者，请不要把这条策略理解为唯一标准答案：</p><ul><li>如果你和本项目一样，主要在 <strong>Mac Apple Silicon</strong> 上离线打包并发布到 <strong>Linux AMD64</strong> 服务器，建议保留当前方案。</li><li>如果你有 <strong>Linux CI/CD</strong>、原生 <strong>AMD64 构建机</strong>，或可以直接在目标 Linux 环境完成构建，通常更建议改回“Linux 原生构建 standalone，再直接复制产物”的标准做法，维护成本会更低。</li><li>如果你更看重“实现简单、尽量少碰打包细节”，也可以回退到“镜像内完整安装生产依赖并使用 <code>pnpm start</code>”的保守方案，只是镜像体积会更大。</li></ul></div><h2 id="a-部署方式总览" tabindex="-1">A. 部署方式总览 <a class="header-anchor" href="#a-部署方式总览" aria-label="Permalink to &quot;A. 部署方式总览&quot;">​</a></h2><p>本项目本质上是一个 <strong>Next.js App Router + Prisma + MySQL + 多个服务端 API Route</strong> 的全栈应用。 因此它不是“只上传静态文件”即可运行的站点，部署时需要同时考虑：</p><ul><li>Node.js 运行环境</li><li>MySQL 数据库可达性</li><li>Prisma Client 生成与迁移</li><li><code>NEXT_PUBLIC_*</code> 构建时注入</li><li>文档预览、插件版本等运行时文件读取能力</li><li>Jenkins / Apifox / 钉钉等外部集成链路</li></ul><h3 id="a-1-方案对比" tabindex="-1">A.1 方案对比 <a class="header-anchor" href="#a-1-方案对比" aria-label="Permalink to &quot;A.1 方案对比&quot;">​</a></h3><table tabindex="0"><thead><tr><th style="${ssrRenderStyle({ "text-align": "left" })}">方式</th><th style="${ssrRenderStyle({ "text-align": "left" })}">适用场景</th><th style="${ssrRenderStyle({ "text-align": "left" })}">优点</th><th style="${ssrRenderStyle({ "text-align": "left" })}">主要问题 / 风险</th><th style="${ssrRenderStyle({ "text-align": "left" })}">推荐度</th></tr></thead><tbody><tr><td style="${ssrRenderStyle({ "text-align": "left" })}">Docker 离线镜像包部署</td><td style="${ssrRenderStyle({ "text-align": "left" })}">旧服务器、内网环境、无法稳定联网拉依赖</td><td style="${ssrRenderStyle({ "text-align": "left" })}">环境一致、可避开宿主机 Glibc/Node 兼容问题、最接近当前项目实践</td><td style="${ssrRenderStyle({ "text-align": "left" })}">需要本地先构建；镜像 tar 较大；需要手动清理磁盘</td><td style="${ssrRenderStyle({ "text-align": "left" })}"><strong>强烈推荐</strong></td></tr><tr><td style="${ssrRenderStyle({ "text-align": "left" })}">Docker 服务器端 build</td><td style="${ssrRenderStyle({ "text-align": "left" })}">服务器能联网，且可访问 Docker Hub / npm</td><td style="${ssrRenderStyle({ "text-align": "left" })}">操作直观，服务器端可直接重建</td><td style="${ssrRenderStyle({ "text-align": "left" })}">仍依赖本地先产出 <code>.next-prod</code>；服务器网络不稳定时容易失败</td><td style="${ssrRenderStyle({ "text-align": "left" })}">推荐</td></tr><tr><td style="${ssrRenderStyle({ "text-align": "left" })}">Vercel 部署</td><td style="${ssrRenderStyle({ "text-align": "left" })}">前端为主、希望快速托管、数据库可公网访问</td><td style="${ssrRenderStyle({ "text-align": "left" })}">上线快、域名/HTTPS/回滚方便、适合公开访问场景</td><td style="${ssrRenderStyle({ "text-align": "left" })}">Serverless 运行时约束更多；数据库连接与函数时长要额外关注；部分运行时读文件能力需验证</td><td style="${ssrRenderStyle({ "text-align": "left" })}">条件满足时可选</td></tr><tr><td style="${ssrRenderStyle({ "text-align": "left" })}">直接 Node.js 部署（非 Docker）</td><td style="${ssrRenderStyle({ "text-align": "left" })}">新服务器环境干净、Node 版本和系统兼容、团队有 Node 运维经验</td><td style="${ssrRenderStyle({ "text-align": "left" })}">部署链路简单、无需容器</td><td style="${ssrRenderStyle({ "text-align": "left" })}">宿主机环境差异大；容易踩 Node/Glibc/依赖兼容问题；与当前项目默认方案不一致</td><td style="${ssrRenderStyle({ "text-align": "left" })}">不推荐作为默认方案</td></tr></tbody></table><h3 id="a-2-快速选择建议" tabindex="-1">A.2 快速选择建议 <a class="header-anchor" href="#a-2-快速选择建议" aria-label="Permalink to &quot;A.2 快速选择建议&quot;">​</a></h3><ul><li>如果你的服务器系统较旧，或你们更看重“稳定可复现”，选 <strong>Docker 离线镜像包部署</strong>。</li><li>如果你们希望保留完整后端能力，并且服务器能稳定联网，选 <strong>Docker 服务器端 build</strong>。</li><li>如果你们希望快速上线到公网，且数据库能提供公网访问或连接代理，可评估 <strong>Vercel</strong>。</li><li>如果只是临时演示，且服务器环境完全可控，可以考虑 <strong>直接 Node.js 部署</strong>，但不建议作为长期标准方案。</li></ul><h3 id="a-3-各方式的常见误区" tabindex="-1">A.3 各方式的常见误区 <a class="header-anchor" href="#a-3-各方式的常见误区" aria-label="Permalink to &quot;A.3 各方式的常见误区&quot;">​</a></h3><h4 id="docker-方案常见误区" tabindex="-1">Docker 方案常见误区 <a class="header-anchor" href="#docker-方案常见误区" aria-label="Permalink to &quot;Docker 方案常见误区&quot;">​</a></h4><ul><li>误以为只更新服务器 <code>.env</code> 就能让前端配置变更生效。 <ul><li>实际上 <code>NEXT_PUBLIC_*</code> 属于构建时注入，必须重新构建并重新部署镜像。</li></ul></li><li>误以为 <code>docker compose up -d</code> 一定会切到新版本。 <ul><li>如果新镜像没有真正 <code>load</code> 成功，容器可能只是基于旧镜像重建。</li></ul></li><li>误以为 <code>pnpm exec prisma generate</code> 就等于“数据库已经更新”。 <ul><li>它只生成 Prisma Client，不会自动执行数据库迁移。</li></ul></li><li>误以为当前仓库的 Docker 打包方式适合所有机器和团队。 <ul><li>实际上这套方案是针对 “Mac Apple Silicon 本地构建 + Linux AMD64 运行” 这一特定组合优化的；如果你有 Linux 原生构建环境，完全可以简化。</li></ul></li></ul><h3 id="a-4-开源使用者如何选择构建策略" tabindex="-1">A.4 开源使用者如何选择构建策略 <a class="header-anchor" href="#a-4-开源使用者如何选择构建策略" aria-label="Permalink to &quot;A.4 开源使用者如何选择构建策略&quot;">​</a></h3><p>如果你不是本项目原始部署环境，而是基于开源仓库自行部署，建议先根据自己的机器和交付方式选择构建策略：</p><table tabindex="0"><thead><tr><th style="${ssrRenderStyle({ "text-align": "left" })}">构建策略</th><th style="${ssrRenderStyle({ "text-align": "left" })}">适用场景</th><th style="${ssrRenderStyle({ "text-align": "left" })}">优点</th><th style="${ssrRenderStyle({ "text-align": "left" })}">代价 / 风险</th></tr></thead><tbody><tr><td style="${ssrRenderStyle({ "text-align": "left" })}">当前默认：本地构建 <code>.next-prod/standalone</code> + Docker 内补齐 Linux 原生依赖</td><td style="${ssrRenderStyle({ "text-align": "left" })}">Apple Silicon Mac 本地打包，目标服务器为 Linux AMD64，且希望保留离线 <code>tar</code> 分发</td><td style="${ssrRenderStyle({ "text-align": "left" })}">避开 Mac 上 <code>buildx/QEMU</code> 执行 <code>next build</code> 的性能和稳定性问题；最终镜像较小</td><td style="${ssrRenderStyle({ "text-align": "left" })}">Dockerfile 依赖 <code>standalone</code> 与 <code>pnpm</code> 内部结构，后续升级 <code>Next.js</code> / <code>Prisma</code> / <code>sharp</code> 时要重点回归</td></tr><tr><td style="${ssrRenderStyle({ "text-align": "left" })}">标准方案：Linux 原生构建 standalone 后直接装箱</td><td style="${ssrRenderStyle({ "text-align": "left" })}">有 Linux CI/CD、AMD64 构建机、或可以在目标 Linux 上原生构建</td><td style="${ssrRenderStyle({ "text-align": "left" })}">结构最标准、维护成本最低、后续升级最稳</td><td style="${ssrRenderStyle({ "text-align": "left" })}">需要额外准备 Linux 构建环境</td></tr><tr><td style="${ssrRenderStyle({ "text-align": "left" })}">保守方案：镜像内完整安装生产依赖并用 <code>pnpm start</code> 运行</td><td style="${ssrRenderStyle({ "text-align": "left" })}">更看重实现直观，接受更大的镜像和 tar</td><td style="${ssrRenderStyle({ "text-align": "left" })}">逻辑最简单，对 <code>standalone</code> 内部结构依赖最低</td><td style="${ssrRenderStyle({ "text-align": "left" })}">镜像与离线包更大，启动和运行时更重</td></tr></tbody></table><p>建议：</p><ul><li>优先看你的 <strong>构建机器架构</strong>，再看你的 <strong>交付方式</strong>。</li><li>如果你长期维护这个仓库，并且会频繁升级依赖，优先考虑 Linux 原生构建方案。</li><li>如果你只是需要快速在 Mac 上打一个可离线分发的 Linux 镜像，当前默认方案更省事。</li></ul><h4 id="vercel-方案常见误区" tabindex="-1">Vercel 方案常见误区 <a class="header-anchor" href="#vercel-方案常见误区" aria-label="Permalink to &quot;Vercel 方案常见误区&quot;">​</a></h4><ul><li>误以为“能跑 Next.js”就一定能零改动迁到 Vercel。 <ul><li>本项目不仅有页面，还有数据库、Webhook、Swagger 聚合、文档预览、插件版本接口等服务端能力。</li></ul></li><li>误以为 Vercel 适合任何数据库拓扑。 <ul><li>如果 MySQL 只允许内网访问，Vercel 无法直接连接，除非你额外提供公网访问或连接代理。</li></ul></li><li>误以为所有本地文件都能在运行时直接读取。 <ul><li>本项目存在读取 <code>docs/</code>、<code>chrome-extension/</code> 等目录文件的接口，迁移到 Vercel 时要确认这些文件被正确打包到函数中。</li></ul></li><li>误以为长耗时同步链路天然适合 Serverless。 <ul><li>Jenkins / Apifox / Swagger Diff / 钉钉通知这类链路要关注函数超时、重试与外部网络稳定性。</li></ul></li></ul><h4 id="直接-node-js-部署常见误区" tabindex="-1">直接 Node.js 部署常见误区 <a class="header-anchor" href="#直接-node-js-部署常见误区" aria-label="Permalink to &quot;直接 Node.js 部署常见误区&quot;">​</a></h4><ul><li>误以为“本地能跑 <code>pnpm dev</code>，服务器就能跑 <code>pnpm start</code>”。 <ul><li>生产环境还涉及 Node 版本、系统库、进程守护、反向代理和日志管理。</li></ul></li><li>误以为直接上传代码后执行 <code>pnpm install &amp;&amp; pnpm build</code> 就结束了。 <ul><li>还需要 <code>.env</code>、数据库迁移、静态资源、重启策略和异常恢复方案。</li></ul></li></ul><h2 id="b-vercel-部署说明-评估用" tabindex="-1">B. Vercel 部署说明（评估用） <a class="header-anchor" href="#b-vercel-部署说明-评估用" aria-label="Permalink to &quot;B. Vercel 部署说明（评估用）&quot;">​</a></h2><p>Vercel <strong>可以作为可选部署方式</strong>，但更适合下面的条件：</p><ul><li>数据库可被公网安全访问，或已经配置连接池 / 代理</li><li>主要诉求是快速上线与托管，而不是深度控制服务器</li><li>接受对部分运行时行为做兼容性处理</li></ul><h3 id="b-1-适合上-vercel-的前提" tabindex="-1">B.1 适合上 Vercel 的前提 <a class="header-anchor" href="#b-1-适合上-vercel-的前提" aria-label="Permalink to &quot;B.1 适合上 Vercel 的前提&quot;">​</a></h3><ul><li>在 Vercel 项目中完整配置 <code>.env</code> 里的生产环境变量</li><li><code>DATABASE_URL</code> 指向 Vercel 可访问的 MySQL</li><li>已确认 Prisma 在生产环境可以正常连接数据库</li><li>已验证运行时读文件相关能力</li><li>已验证 Jenkins / Apifox / 钉钉等外部链路不会因函数超时失败</li></ul><h3 id="b-2-迁移到-vercel-时重点自查" tabindex="-1">B.2 迁移到 Vercel 时重点自查 <a class="header-anchor" href="#b-2-迁移到-vercel-时重点自查" aria-label="Permalink to &quot;B.2 迁移到 Vercel 时重点自查&quot;">​</a></h3><ol><li><p>数据库</p><ul><li>MySQL 是否允许公网访问？</li><li>是否需要连接池或数据库代理？</li></ul></li><li><p>文件读取</p><ul><li>文档预览接口会读取 <code>docs/*.md</code></li><li>插件版本和系统配置接口会读取 <code>chrome-extension/manifest.json</code></li><li>这些文件在 Vercel 函数中是否可用，需要上线后实测</li></ul></li><li><p>外部集成</p><ul><li><code>PUBLIC_URL</code> 是否改成 Vercel 域名？</li><li>Webhook 来源地址、回调地址、白名单是否同步调整？</li></ul></li><li><p>数据库迁移</p><ul><li>不要只执行 <code>prisma generate</code></li><li>需要明确生产数据库初始化/迁移策略</li></ul></li></ol><h3 id="b-3-不建议优先选-vercel-的情况" tabindex="-1">B.3 不建议优先选 Vercel 的情况 <a class="header-anchor" href="#b-3-不建议优先选-vercel-的情况" aria-label="Permalink to &quot;B.3 不建议优先选 Vercel 的情况&quot;">​</a></h3><ul><li>数据库只能内网访问</li><li>项目高度依赖公司内网 Jenkins / 服务治理 / 内部系统</li><li>需要较稳定地执行较长时间的同步或聚合任务</li><li>团队更熟悉服务器 / Docker 运维，而不是 Serverless 运行时问题排查</li></ul><h2 id="c-直接-node-js-部署说明-不推荐默认使用" tabindex="-1">C. 直接 Node.js 部署说明（不推荐默认使用） <a class="header-anchor" href="#c-直接-node-js-部署说明-不推荐默认使用" aria-label="Permalink to &quot;C. 直接 Node.js 部署说明（不推荐默认使用）&quot;">​</a></h2><p>如果你确实要跳过 Docker，至少要满足以下条件：</p><ul><li>服务器系统能兼容当前 Node 版本</li><li>能稳定执行 <code>pnpm install</code>、<code>pnpm build</code></li><li>已准备进程守护（如 <code>pm2</code> / <code>systemd</code>）</li><li>已准备反向代理（如 Nginx）</li><li>已明确数据库迁移和日志管理方式</li></ul><p>更重要的是：本项目当前的主文档、排障经验和交付流程都以 Docker 为基准，因此直接 Node.js 部署的维护成本通常更高。</p><h2 id="_0-文件说明-建议必读" tabindex="-1">0. 文件说明（建议必读） <a class="header-anchor" href="#_0-文件说明-建议必读" aria-label="Permalink to &quot;0. 文件说明（建议必读）&quot;">​</a></h2><ul><li><code>deploy/docker/docker-compose.yml</code>：偏开发/本机使用（包含 <code>build:</code>）。</li><li><code>deploy/docker/docker-compose.prod.yml</code>：偏服务器使用（应为 <code>image: dev-portal:latest</code>，配合离线镜像包部署）。</li><li><code>deploy/docker/Dockerfile</code>：Docker 构建文件。构建时请显式使用 <code>-f deploy/docker/Dockerfile</code>，并保持项目根目录 <code>.</code> 作为上下文。</li><li><code>deploy/pm2/ecosystem.config.js</code>：PM2 生产启动配置。根目录保留 <code>ecosystem.config.js</code> 兼容包装入口。</li><li><code>deploy/scripts/server-deploy.sh</code>：实际部署脚本；根目录保留 <code>server-deploy.sh</code> 兼容包装入口。脚本会优先查找根目录旧 compose 文件，也兼容 <code>deploy/docker/*.yml</code> 与 <code>artifacts/docker/dev-portal.tar</code>。</li></ul><h2 id="_1-前置要求" tabindex="-1">1. 前置要求 <a class="header-anchor" href="#_1-前置要求" aria-label="Permalink to &quot;1. 前置要求&quot;">​</a></h2><p>确保服务器已安装 Docker。 由于 Docker 版本较新 (24+)，我们将使用内置的 <strong>Docker Compose V2</strong> 插件。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 检查是否安装</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> compose</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> version</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 如果报错 &#39;docker: &#39;compose&#39; is not a docker command&#39;，则尝试旧版命令：</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># docker-compose version</span></span></code></pre></div><h2 id="_2-部署步骤" tabindex="-1">2. 部署步骤 <a class="header-anchor" href="#_2-部署步骤" aria-label="Permalink to &quot;2. 部署步骤&quot;">​</a></h2><ol><li><p><strong>推荐方式：离线镜像包部署（最稳定）</strong>:</p><ul><li>在本地执行 <code>pnpm docker:pack</code> 生成 <code>artifacts/docker/dev-portal.tar</code>（不会影响本地开发用的 <code>.next</code>）。</li><li>上传到服务器时，建议保持以下结构：镜像包放在项目根目录，compose 文件放在 <code>deploy/docker/</code>，再配合根目录 <code>.env</code> 与可选的 <code>server-deploy.sh</code> 一起使用。</li><li>服务器侧（不使用脚本时）执行：<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> load</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -i</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev-portal.tar</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> compose</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> deploy/docker/docker-compose.prod.yml</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> up</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -d</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --force-recreate</span></span></code></pre></div></li><li>服务器侧（使用脚本时）执行：<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">chmod</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> +x</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> server-deploy.sh</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">./server-deploy.sh</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 选择「2. 更新应用（加载 tar 并重建）」</span></span></code></pre></div></li></ul></li><li><p><strong>备选方式：服务器端 docker build（不推荐）</strong>:</p><ul><li>你仍然需要先在本地执行 <code>pnpm build:prod</code>，并将生成的目录 <strong>保持为 <code>.next-prod</code>（不要改名）</strong> 一起上传到服务器。</li><li>然后在服务器项目根目录中运行 <code>docker compose -f deploy/docker/docker-compose.yml up -d --build</code>。</li><li>注意：如果你修改的是 <code>NEXT_PUBLIC_*</code>（例如 <code>NEXT_PUBLIC_DEFAULT_APPS</code>），它属于“构建时注入”，仅改服务器 <code>.env</code> 不会让前端生效，必须重新打包镜像。</li></ul></li><li><p><strong>构建并启动（仅当你不使用 server-deploy.sh 时）</strong>: 进入项目根目录并运行：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 开发 / 本机构建</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> compose</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> deploy/docker/docker-compose.yml</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> up</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -d</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 生产 / 离线镜像包</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> compose</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> deploy/docker/docker-compose.prod.yml</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> up</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -d</span></span></code></pre></div><p><em>(如果是第一次运行，构建过程可能需要几分钟)</em></p></li><li><p><strong>验证</strong>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> compose</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> deploy/docker/docker-compose.prod.yml</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ps</span></span></code></pre></div><p>状态应为 <code>Up</code>。访问 <code>http://服务器IP:3001</code> 即可。</p></li></ol><h2 id="_2-1-验证与排障-强烈建议" tabindex="-1">2.1 验证与排障（强烈建议） <a class="header-anchor" href="#_2-1-验证与排障-强烈建议" aria-label="Permalink to &quot;2.1 验证与排障（强烈建议）&quot;">​</a></h2><h3 id="_2-1-1-验证-是否真的更新成功" tabindex="-1">2.1.1 验证“是否真的更新成功” <a class="header-anchor" href="#_2-1-1-验证-是否真的更新成功" aria-label="Permalink to &quot;2.1.1 验证“是否真的更新成功”&quot;">​</a></h3><p>离线部署时，务必确认 <strong>新 tar 已成功 load</strong>、且 <strong>容器已使用新镜像重建</strong>：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 1) 上传文件是否完整（大小/时间是否符合预期）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ls</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -lh</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev-portal.tar</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 2) load 成功标志：输出应包含 &quot;Loaded image: dev-portal:latest&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> load</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -i</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev-portal.tar</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 3) 镜像是否更新（Id/Created 应变化）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> image</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> inspect</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev-portal:latest</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --format</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;{{.Id}} {{.Created}}&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 4) 容器是否已切到新镜像（Image ID 应与上面一致）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> inspect</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev-portal</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --format</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;{{.Image}} {{.Config.Image}} {{.State.StartedAt}}&#39;</span></span></code></pre></div><div class="tip custom-block github-alert"><p class="custom-block-title">TIP</p><p>如果 <code>docker load</code> 失败，但你仍继续执行了 <code>docker compose up ...</code>，最终只会“重建旧容器”，页面看起来就会“一直没变化”。</p></div><h3 id="_2-1-2-常见问题-docker-load-报-no-space-left-on-device" tabindex="-1">2.1.2 常见问题：<code>docker load</code> 报 <code>no space left on device</code> <a class="header-anchor" href="#_2-1-2-常见问题-docker-load-报-no-space-left-on-device" aria-label="Permalink to &quot;2.1.2 常见问题：\`docker load\` 报 \`no space left on device\`&quot;">​</a></h3><p>这代表服务器磁盘空间不足（常见在 Docker 数据目录所在分区）。建议按顺序处理：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 看磁盘/镜像占用</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">df</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -h</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> system</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> df</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 清理无用资源（按需，慎用 -a）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> container</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> prune</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -f</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> image</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> prune</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -f</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> builder</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> prune</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -f</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># docker image prune -a -f</span></span></code></pre></div><h3 id="_2-1-3-常见原因-反复-docker-load-导致旧镜像堆积" tabindex="-1">2.1.3 常见原因：反复 <code>docker load</code> 导致旧镜像堆积 <a class="header-anchor" href="#_2-1-3-常见原因-反复-docker-load-导致旧镜像堆积" aria-label="Permalink to &quot;2.1.3 常见原因：反复 \`docker load\` 导致旧镜像堆积&quot;">​</a></h3><p>即使每次加载的都是同一个 tag（例如 <code>dev-portal:latest</code>），<code>docker load</code> 也会导入一份“新的镜像内容”，原先的镜像会变成 <strong>dangling（无 tag）</strong>，如果不清理会持续占用磁盘。</p><p>排查与处理建议：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 1) 查看 dev-portal 镜像与大小</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> image</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ls</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> head</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> image</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ls</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> rg</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;dev-portal&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 2) 查看 dangling 镜像数量/大小（重点）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> image</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ls</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dangling=</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 3) 安全清理：只删 dangling（推荐）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> image</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> prune</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -f</span></span></code></pre></div><div class="tip custom-block github-alert"><p class="custom-block-title">TIP</p><p>如果你使用 <code>server-deploy.sh</code>，在“更新应用（加载 tar 并重建）”后会提示是否清理， 也可以在菜单中选择「6. 清理未使用镜像」。</p></div><h3 id="_2-1-4-常见误区-只改服务器-env-但前端没变化" tabindex="-1">2.1.4 常见误区：只改服务器 <code>.env</code> 但前端没变化 <a class="header-anchor" href="#_2-1-4-常见误区-只改服务器-env-但前端没变化" aria-label="Permalink to &quot;2.1.4 常见误区：只改服务器 \`.env\` 但前端没变化&quot;">​</a></h3><p>如果你修改的是 <code>NEXT_PUBLIC_*</code>（例如 <code>NEXT_PUBLIC_DEFAULT_APPS</code>），它属于 <strong>构建时注入</strong>：</p><ul><li>仅在服务器修改 <code>.env</code> 不会改变已打包进镜像的前端内容；</li><li>需要重新在本地执行 <code>pnpm docker:pack</code>，并上传新的镜像包（本地默认位于 <code>artifacts/docker/dev-portal.tar</code>）再更新。</li></ul><h2 id="_3-常用命令" tabindex="-1">3. 常用命令 <a class="header-anchor" href="#_3-常用命令" aria-label="Permalink to &quot;3. 常用命令&quot;">​</a></h2><ul><li><strong>查看日志</strong>:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> compose</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> deploy/docker/docker-compose.prod.yml</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> logs</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -f</span></span></code></pre></div></li><li><strong>重启服务</strong>:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> compose</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> deploy/docker/docker-compose.prod.yml</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> restart</span></span></code></pre></div></li><li><strong>停止服务</strong>:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> compose</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> deploy/docker/docker-compose.prod.yml</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> down</span></span></code></pre></div></li><li><strong>更新代码后重新部署</strong>:<div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 方式 A（离线镜像包）：上传新的 dev-portal.tar 后</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> load</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -i</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev-portal.tar</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> compose</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> deploy/docker/docker-compose.prod.yml</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> up</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -d</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --force-recreate</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 方式 B（服务器端 build）：拉取/更新代码后</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># docker compose -f deploy/docker/docker-compose.yml up -d --build</span></span></code></pre></div></li></ul><h2 id="_3-1-prisma-数据库迁移-强烈建议保留" tabindex="-1">3.1 Prisma 数据库迁移（强烈建议保留） <a class="header-anchor" href="#_3-1-prisma-数据库迁移-强烈建议保留" aria-label="Permalink to &quot;3.1 Prisma 数据库迁移（强烈建议保留）&quot;">​</a></h2><div class="important custom-block github-alert"><p class="custom-block-title">IMPORTANT</p><p><code>pnpm exec prisma generate</code> <strong>只会生成 Prisma Client（类型/代码）</strong>，不会建表/加字段/删字段。 真正让数据库结构“从 0 变成可用”，需要跑迁移（migrations）。</p></div><h3 id="开发环境-本地" tabindex="-1">开发环境（本地） <a class="header-anchor" href="#开发环境-本地" aria-label="Permalink to &quot;开发环境（本地）&quot;">​</a></h3><p>当你修改了 <code>prisma/schema.prisma</code>（例如新增字段）：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 生成并应用迁移（会连接数据库）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> prisma</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> migrate</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 如果遇到 Prisma Client 字段不一致（例如提示 Unknown argument），可手动再生成一次</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> prisma</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> generate</span></span></code></pre></div><div class="note custom-block github-alert"><p class="custom-block-title">NOTE</p><p>如果你本地正在跑 <code>pnpm dev</code>，改完 schema 后仍然报 “Unknown argument xxx”，通常是旧 Prisma Client/旧 dev 进程未刷新： 先停掉 <code>pnpm dev</code> 再重新启动即可。</p></div><h3 id="生产-服务器-docker" tabindex="-1">生产/服务器（Docker） <a class="header-anchor" href="#生产-服务器-docker" aria-label="Permalink to &quot;生产/服务器（Docker）&quot;">​</a></h3><p>部署时（无论数据库是空库还是已有数据），建议用 <strong>deploy</strong> 模式执行迁移：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 只会应用 prisma/migrations 中尚未执行的迁移（不会生成新迁移）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> prisma</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> migrate</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> deploy</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 确保 Prisma Client 与 schema 一致</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> prisma</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> generate</span></span></code></pre></div><div class="tip custom-block github-alert"><p class="custom-block-title">TIP</p><p>如果你们确定“每次都是全新空库”，理论上可以用 <code>pnpm exec prisma db push</code> 直接同步结构； 但它没有迁移历史、不利于排查与回溯，所以本项目选择保留 migrations。</p></div><h2 id="_3-2-swagger-自动同步上线后校验-新增" tabindex="-1">3.2 Swagger 自动同步上线后校验（新增） <a class="header-anchor" href="#_3-2-swagger-自动同步上线后校验-新增" aria-label="Permalink to &quot;3.2 Swagger 自动同步上线后校验（新增）&quot;">​</a></h2><p>完成部署后，建议执行一次最小校验：</p><ol><li>执行迁移并重启服务，确认存在 <code>apifox_spec_snapshot</code> 表。</li><li>打开 <code>Swagger Efficiency Kit -&gt; CI/CD 自动化插件</code>，点击 <code>初始化基线</code>。</li><li>触发一次 Jenkins 自动化推送： <ol><li>第一次通常会建立或覆盖基线。</li><li>第二次开始应在钉钉中看到 Diff 摘要与接口路径明细。</li></ol></li><li>如初始化时报错 <code>Cannot read properties of undefined (reading &#39;upsert&#39;)</code>，通常是 Prisma Client 未刷新： <ol><li>重新执行 <code>pnpm exec prisma generate</code>。</li><li>重启应用或重建容器。</li></ol></li></ol><h2 id="_4-可选-清理无效的-nvm-环境" tabindex="-1">4. (可选) 清理无效的 NVM 环境 <a class="header-anchor" href="#_4-可选-清理无效的-nvm-环境" aria-label="Permalink to &quot;4. (可选) 清理无效的 NVM 环境&quot;">​</a></h2><p>由于我们已切换到 Docker 部署，宿主机上之前安装的 NVM 和 Node (因 Glibc 版本过低无法使用) 可以安全清理。</p><p><strong>清理步骤</strong>:</p><ol><li><p><strong>删除 NVM 目录</strong>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">rm</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -rf</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ~/.nvm</span></span></code></pre></div></li><li><p><strong>清理 Shell 配置</strong>: 编辑 <code>~/.bashrc</code> (或 <code>~/.zshrc</code>)，删除以下 NVM 初始化代码：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> NVM_DIR</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">$HOME</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/.nvm&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[ </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">-s</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">$NVM_DIR</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/nvm.sh&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> ] &amp;&amp; </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">\\.</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">$NVM_DIR</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">/nvm.sh&quot;</span></span></code></pre></div></li><li><p><strong>生效</strong>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">source</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ~/.bashrc</span></span></code></pre></div></li></ol><h2 id="_5-故障排除-docker-镜像拉取超时" tabindex="-1">5. (故障排除) Docker 镜像拉取超时 <a class="header-anchor" href="#_5-故障排除-docker-镜像拉取超时" aria-label="Permalink to &quot;5. (故障排除) Docker 镜像拉取超时&quot;">​</a></h2><p>如果构建时卡在 <code>load metadata for docker.io/library/node:20-alpine</code>，说明服务器无法连接 Docker Hub。 请配置国内镜像加速器。</p><p><strong>操作步骤</strong>:</p><ol><li><p><strong>编辑配置</strong>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">mkdir</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -p</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /etc/docker</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">vim</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> /etc/docker/daemon.json</span></span></code></pre></div></li><li><p><strong>写入以下内容</strong> (使用国内可用源):</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;registry-mirrors&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">    &quot;https://docker.m.daocloud.io&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">    &quot;https://huecker.io&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">    &quot;https://dockerhub.timeweb.cloud&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">    &quot;https://noohub.ru&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  ]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div></li><li><p><strong>重启 Docker</strong>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">systemctl</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> daemon-reload</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">systemctl</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> restart</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> docker</span></span></code></pre></div></li></ol><h2 id="_6-替代方案-本地构建并上传-离线部署" tabindex="-1">6. (替代方案) 本地构建并上传 (离线部署) <a class="header-anchor" href="#_6-替代方案-本地构建并上传-离线部署" aria-label="Permalink to &quot;6. (替代方案) 本地构建并上传 (离线部署)&quot;">​</a></h2><p>如果服务器网络实在太差，您可以在 <strong>本地电脑</strong> 构建好镜像，然后上传到服务器。 <em>(注意：需要本地也安装 Docker)</em></p><h3 id="方式-a-使用-npm-快捷命令-推荐" tabindex="-1">方式 A: 使用 NPM 快捷命令 (推荐) <a class="header-anchor" href="#方式-a-使用-npm-快捷命令-推荐" aria-label="Permalink to &quot;方式 A: 使用 NPM 快捷命令 (推荐)&quot;">​</a></h3><p>我们在 <code>package.json</code> 中配置了快捷脚本，您只需运行：</p><ol><li><p><strong>一键构建并打包</strong>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> docker:pack</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 该命令会自动运行 pnpm build:prod 并打包成 artifacts/docker/dev-portal.tar</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 且不会影响您本地正在运行的开发环境 (.next)</span></span></code></pre></div></li><li><p><strong>上传文件</strong>: 您需要上传可以通过离线部署的 <strong>两个核心文件</strong>：</p><ul><li><code>artifacts/docker/dev-portal.tar</code> (镜像包)</li><li><code>deploy/docker/docker-compose.prod.yml</code> (<strong>生产环境专用配置</strong>)</li></ul><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">ssh</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> root@your-server-ip</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;mkdir -p /root/project/deploy/docker&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">scp</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> artifacts/docker/dev-portal.tar</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> root@your-server-ip:/root/project/dev-portal.tar</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">scp</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> deploy/docker/docker-compose.prod.yml</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> root@your-server-ip:/root/project/deploy/docker/docker-compose.prod.yml</span></span></code></pre></div></li></ol><h3 id="方式-b-手动执行命令" tabindex="-1">方式 B: 手动执行命令 <a class="header-anchor" href="#方式-b-手动执行命令" aria-label="Permalink to &quot;方式 B: 手动执行命令&quot;">​</a></h3><ol><li><p><strong>本地构建 (指定 x86 架构)</strong>:</p><div class="important custom-block github-alert"><p class="custom-block-title">&gt; **Mac M1/M2/M3 (Apple Silicon) 用户必须保留 \`--platform linux/amd64\` 参数**。</p><p>否则构建出的镜像（ARM 架构）将无法在普通 Linux 服务器（通常是 x86/AMD64 架构）上运行。</p></div><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 在项目根目录执行</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> buildx</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> build</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --platform</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> linux/amd64</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --load</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> deploy/docker/Dockerfile</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -t</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev-portal:latest</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> .</span></span></code></pre></div></li><li><p><strong>导出镜像</strong>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">mkdir</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -p</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> artifacts/docker</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> save</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -o</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> artifacts/docker/dev-portal.tar</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev-portal:latest</span></span></code></pre></div></li><li><p><strong>上传到服务器</strong>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 使用 scp 或其他工具</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">scp</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> artifacts/docker/dev-portal.tar</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> root@your-server-ip:/root/dev-portal.tar</span></span></code></pre></div></li><li><p><strong>服务器导入</strong>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> load</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -i</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev-portal.tar</span></span></code></pre></div></li><li><p><strong>使用生产 compose 启动</strong>: 服务器侧直接使用已经准备好的 <code>deploy/docker/docker-compose.prod.yml</code>，无需再手工注释 <code>build</code> 配置：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> compose</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> deploy/docker/docker-compose.prod.yml</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> up</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -d</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --force-recreate</span></span></code></pre></div></li></ol><h3 id="🚀-极简运维-推荐" tabindex="-1">🚀 极简运维 (推荐) <a class="header-anchor" href="#🚀-极简运维-推荐" aria-label="Permalink to &quot;🚀 极简运维 (推荐)&quot;">​</a></h3><p>我们为您准备了 <code>server-deploy.sh</code> 脚本，将它与镜像包一起上传到服务器，然后执行：</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">chmod</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> +x</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> server-deploy.sh</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">./server-deploy.sh</span></span></code></pre></div><p>会出现交互式中文菜单，您可以选择：</p><ol><li><strong>首次部署（初始化）</strong>：自动检查 <code>.env</code>、加载 <code>dev-portal.tar</code> 并启动服务。</li><li><strong>更新应用（加载 tar 并重建）</strong>：上传新的 <code>dev-portal.tar</code> 后使用；更新前会询问是否需要备份（默认不备份）。</li><li><strong>仅重启服务</strong>：不更新镜像，仅重启容器。</li><li><strong>查看日志（Ctrl+C 返回）</strong>：进入 <code>docker compose logs -f</code> 跟随日志，按 <code>Ctrl+C</code> 返回菜单。</li><li><strong>进入容器 Shell（exit 返回）</strong>：进入容器后输入 <code>exit</code> 返回菜单。</li><li><strong>清理未使用镜像</strong>：执行 <code>docker image prune -f</code>。</li><li><strong>备份当前版本（镜像 + 配置 + tar）</strong>：备份到 <code>./backups/&lt;时间戳&gt;/</code>。</li><li><strong>回滚到备份版本</strong>：从 <code>./backups/</code> 选择一个备份回滚（可选同时恢复 <code>.env/compose</code>）。</li></ol><p>每次执行完一项操作后，脚本会询问是否继续；默认不继续并自动退出。</p><h4 id="备份与空间占用说明" tabindex="-1">备份与空间占用说明 <a class="header-anchor" href="#备份与空间占用说明" aria-label="Permalink to &quot;备份与空间占用说明&quot;">​</a></h4><ul><li>备份目录：<code>./backups/YYYYMMDD-HHMMSS/</code></li><li>环境检查：脚本会检查 <code>.env</code> 中的 <code>DEVPORTAL_EXTENSION_API_KEY</code>，未配置会提示输入（否则插件可能提示 <code>Server not configured</code>）。</li><li>备份内容（尽力而为，缺少就跳过）： <ul><li><code>dev-portal.tar</code>（如果当前目录存在，会复制一份进去）</li><li><code>image.tar</code>（如果服务器上存在 <code>dev-portal:latest</code> 镜像，会 <code>docker save</code> 备份一份）</li><li><code>.env</code> 与 <code>deploy/docker/docker-compose*.yml</code></li></ul></li><li>空间占用：<code>dev-portal.tar</code> 与 <code>image.tar</code> 体积通常同量级（都可能比较大），因此默认只保留最近 1 份备份。</li><li>保留策略：脚本内置 <code>BACKUP_KEEP=1</code>，每次备份后会自动清理旧备份（只会清理 <code>./backups/</code> 下符合时间戳格式的目录，不会影响容器、数据库或其他文件）。</li></ul><h3 id="🔄-服务器端如何更新-重启" tabindex="-1">🔄 服务器端如何更新 (重启)? <a class="header-anchor" href="#🔄-服务器端如何更新-重启" aria-label="Permalink to &quot;🔄 服务器端如何更新 (重启)?&quot;">​</a></h3><p>当您上传了新的 <code>dev-portal.tar</code> 到服务器后，请按以下步骤更新服务：</p><ol><li><p><strong>导入新镜像</strong>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> load</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -i</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> dev-portal.tar</span></span></code></pre></div></li><li><p><strong>重启服务 (加载新镜像)</strong>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">docker</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> compose</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -f</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> deploy/docker/docker-compose.prod.yml</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> up</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -d</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --force-recreate</span></span></code></pre></div><p><em>(注：核心是让容器基于新加载的镜像强制重建；<code>--force-recreate</code> 比手工 <code>down</code> 再 <code>up</code> 更直接，也更不容易漏掉 compose 文件路径。)</em></p></li></ol><h2 id="_7-附录-技术原理-为什么这样快且稳" tabindex="-1">7. (附录) 技术原理：为什么这样快且稳？ <a class="header-anchor" href="#_7-附录-技术原理-为什么这样快且稳" aria-label="Permalink to &quot;7. (附录) 技术原理：为什么这样快且稳？&quot;">​</a></h2><p>为了解决 Mac Apple Silicon (ARM) 模拟 Linux (x86) 构建慢且易崩溃的问题，我们采用了 <strong>“本地构建 standalone + Docker 内补齐 Linux 原生依赖”</strong> 策略。</p><h3 id="核心流程可视化" tabindex="-1">核心流程可视化 <a class="header-anchor" href="#核心流程可视化" aria-label="Permalink to &quot;核心流程可视化&quot;">​</a></h3><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">graph TD</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    %% Define Styles</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    classDef local fill:#e6f7ff,stroke:#1890ff,stroke-width:2px;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    classDef server fill:#f6ffed,stroke:#52c41a,stroke-width:2px;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    classDef artifact fill:#fff7e6,stroke:#fa8c16,stroke-width:2px,stroke-dasharray: 5 5;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    subgraph Local [&quot;💻 本地环境 (Mac M-Chip)&quot;]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        direction TB</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        Code[Source Code] --&gt; |1. pnpm build:prod| NextDist[.next-prod 文件夹]:::artifact</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        NextDist --&gt; |2. COPY standalone + static| DockerBuild[&quot;Docker Build (x86)&quot;]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        Pkg[package.json / prisma] --&gt; |3. 仅生成 Linux Prisma / sharp 运行时依赖| DockerBuild</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        DockerBuild --&gt; |4. docker save| TarFile[artifacts/docker/dev-portal.tar]:::artifact</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    end</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    TarFile --&gt; |5. scp 上传| ServerEnv</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    Config[deploy/docker/docker-compose.prod.yml] --&gt; |5. scp 上传| ServerEnv</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    subgraph ServerEnv [&quot;☁️ 生产服务器 (Linux)&quot;]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        direction TB</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        LoadedImage[Loaded Image]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        RunningContainer[🟢 Running Container]</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        TarFile --&gt; |6. docker load| LoadedImage</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        Config --&gt; |7. docker compose up| RunningContainer</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">        LoadedImage -.-&gt; RunningContainer</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    end</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    class Local local</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    class ServerEnv server</span></span></code></pre></div><h3 id="构建流程图解" tabindex="-1">构建流程图解 <a class="header-anchor" href="#构建流程图解" aria-label="Permalink to &quot;构建流程图解&quot;">​</a></h3><ol><li><p><strong>本地编译 (Local Build)</strong>:</p><ul><li>在您的 Mac 上利用原生 CPU 性能执行 <code>pnpm build:prod</code>。</li><li><strong>产出</strong>: <code>.next-prod</code> 文件夹（包含 <code>standalone</code>、<code>static</code> 以及通用的 JS/CSS/HTML 产物）。</li><li><strong>隔离</strong>: 此过程<strong>不影响</strong>您本地 <code>.next</code> 目录（即不影响 <code>pnpm dev</code>）。</li><li><em>注意：此时构建产物中的原生依赖仍然带有宿主机平台特征，不能直接假设适用于 Linux。</em></li></ul></li><li><p><strong>Docker 原生依赖生成 (Container Native Deps)</strong>:</p><ul><li>Docker 构建时，不会重新执行 <code>next build</code>。</li><li>它会在 Linux x86 环境中单独执行依赖安装与 <code>prisma generate</code>，提取 Linux 版的 Prisma / sharp 运行时文件。</li><li><strong>产出</strong>: 仅包含 Linux 原生运行时所需的依赖目录，而不是完整重建整个应用。</li></ul></li><li><p><strong>运行时装箱 (Runtime Packaging)</strong>:</p><ul><li>最终镜像复制第 1 步生成的 <code>.next-prod/standalone</code> 与 <code>.next-prod/static</code>。</li><li>然后用第 2 步生成的 Linux 原生运行时文件替换构建产物中的宿主机版本。</li><li>结果：获得一个既包含最新业务代码、又能在 Linux 上正确运行 Prisma / sharp 的镜像。</li></ul></li></ol><h3 id="_7-1-这不是唯一方案" tabindex="-1">7.1 这不是唯一方案 <a class="header-anchor" href="#_7-1-这不是唯一方案" aria-label="Permalink to &quot;7.1 这不是唯一方案&quot;">​</a></h3><p>对于开源使用者，请将本节理解为“当前仓库默认策略的设计原因”，而不是唯一的部署标准：</p><ul><li>如果你直接在 Linux AMD64 环境构建，本节这套“补齐 Linux 原生依赖”的额外复杂度通常可以省掉。</li><li>如果你更看重可维护性而不是镜像大小，也可以回到 <code>pnpm start</code> 的传统方案。</li><li>如果你修改了 <code>Next.js</code>、<code>Prisma</code>、<code>sharp</code> 版本，建议重新验证 Docker 打包流程，而不要假设当前 Dockerfile 永远无需调整。</li></ul><h2 id="_8-公司内网部署与分发指南" tabindex="-1">8. 公司内网部署与分发指南 <a class="header-anchor" href="#_8-公司内网部署与分发指南" aria-label="Permalink to &quot;8. 公司内网部署与分发指南&quot;">​</a></h2><p>如果您需要在公司内部推广使用本系统，请参考以下流程：</p><h3 id="_8-1-服务端部署-后端" tabindex="-1">8.1 服务端部署 (后端) <a class="header-anchor" href="#_8-1-服务端部署-后端" aria-label="Permalink to &quot;8.1 服务端部署 (后端)&quot;">​</a></h3><p>请按照本文档第 2 节或第 6 节的步骤，将服务部署在公司内网服务器上（例如 <code>192.168.x.x</code>）。 假设部署后的服务地址为：<code>http://192.168.1.100:3001</code></p><h3 id="_8-2-chrome-扩展打包与分发-客户端" tabindex="-1">8.2 Chrome 扩展打包与分发 (客户端) <a class="header-anchor" href="#_8-2-chrome-扩展打包与分发-客户端" aria-label="Permalink to &quot;8.2 Chrome 扩展打包与分发 (客户端)&quot;">​</a></h3><p>为了让同事们无需安装 Git 或 Node.js 环境也能使用扩展，您需要打包并分发配置好的扩展程序。</p><h4 id="第一步-修改-api-地址" tabindex="-1">第一步：修改 API 地址 <a class="header-anchor" href="#第一步-修改-api-地址" aria-label="Permalink to &quot;第一步：修改 API 地址&quot;">​</a></h4><p>打开代码中的 <code>chrome-extension/popup.js</code> 文件，将顶部的 <code>API_URL</code> 修改为内网服务器地址：</p><div class="language-javascript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// chrome-extension/popup.js</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// const API_URL = &quot;http://localhost:3000/api/match-credentials&quot;;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> API_URL</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;http://192.168.1.100:3001/api/match-credentials&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// &lt;--- 修改这里</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> API_KEY</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &quot;YOUR_SHARED_KEY&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">; </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// &lt;--- 同时配置 Key（与服务端 DEVPORTAL_EXTENSION_API_KEY 一致）</span></span></code></pre></div><h4 id="第二步-打包扩展" tabindex="-1">第二步：打包扩展 <a class="header-anchor" href="#第二步-打包扩展" aria-label="Permalink to &quot;第二步：打包扩展&quot;">​</a></h4><ol><li>进入 <code>chrome-extension</code> 目录。</li><li>将该目录下的所有文件（<code>manifest.json</code>, <code>popup.html</code>, <code>popup.js</code>, <code>background.js</code>, <code>README.md</code> 等）打包成一个 <code>.zip</code> 压缩包。</li><li>命名建议：<code>DevPortal-Extension-v1.0.zip</code>。</li></ol><h4 id="第三步-分发与安装" tabindex="-1">第三步：分发与安装 <a class="header-anchor" href="#第三步-分发与安装" aria-label="Permalink to &quot;第三步：分发与安装&quot;">​</a></h4><ol><li>将 <code>DevPortal-Extension-v1.0.zip</code> 发送给同事，或上传到公司网盘。</li><li><strong>同事需执行的操作</strong>： <ul><li>解压 <code>.zip</code> 包到一个固定文件夹。</li><li>打开 Chrome 浏览器，访问 <code>chrome://extensions/</code>。</li><li>开启右上角的 <strong>“开发者模式”</strong>。</li><li>点击左上角的 <strong>“加载已解压的扩展程序”</strong>，选择解压后的文件夹。</li><li>推荐点击浏览器工具栏的“拼图”图标，将插件 <strong>固定 (Pin)</strong> 在工具栏上。</li></ul></li></ol><hr><p>Powered by Next.js &amp; Prisma</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/deploy.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const deploy = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  deploy as default
};
