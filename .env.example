# Database Connection
# 格式: mysql://USER:PASSWORD@HOST:PORT/DATABASE
# 注意: 如果使用 docker-compose.standalone.yml启动，HOST 应设为 db (容器内部互联) 或 localhost (如果从外部连接映射端口)
# 默认配置适配 docker-compose.standalone.yml
# 示例 (Docker 内部): mysql://root:${MYSQL_ROOT_PASSWORD}@db:3306/project_nav
# 示例 (本地开发): mysql://root:${MYSQL_ROOT_PASSWORD}@localhost:33060/project_nav
DATABASE_URL="mysql://root:rootpassword@localhost:33060/project_nav"

# MySQL Root Password (required for docker-compose)
MYSQL_ROOT_PASSWORD="rootpassword"

# App Environment
NODE_ENV="development"
